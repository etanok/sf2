{% extends "AutoformationBlogBundle::layout.html.twig" %}
{% block title %}
    Lecture d'un article - {{ parent() }}
{% endblock %}
{% block autoformation_blog_body %}
    {# Ici, on inclut la vue #}
    {% include "AutoformationBlogBundle:Blog:article.html.twig" %}
    <p>
        <a href="{{ path('autoformation_blog_accueil') }}" class="btn">
            <i class="icon-chevron-left"></i>
            Retour à la liste
        </a>
        <a href="{{ path('autoformation_blog_modifier', {'id': article.id}) }}"
           class="btn">
            <i class="icon-edit"></i>
            Modifier l'article
        </a>
        <a href="{{ path('autoformation_blog_supprimer', {'id': article.id}) }}"
           class="btn">
            <i class="icon-trash"></i>
            Supprimer l'article
        </a>
    </p>
{% endblock %}